#!/usr/bin/env bash
# Note: The run a single command inside the virtual environment (such as pytest)
# refer to the following example code.
#           ex. 
#               > poetry run pytest
poetry shell
